import { OrderUsersByRankingPipe } from './order-users-by-ranking.pipe';

describe('OrderUsersByRankingPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderUsersByRankingPipe();
    expect(pipe).toBeTruthy();
  });
});
